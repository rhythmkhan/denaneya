#!/usr/bin/env sh
DIR="$(cd "$(dirname "$0")" && pwd)"
GRADLE_BIN="/c/Users/mdama/.gradle/wrapper/dists/gradle-9.3.1-bin/23ovyewtku6u96viwx3xl3oks/gradle-9.3.1/bin/gradle"
if [ -f "$GRADLE_BIN" ]; then
    "$GRADLE_BIN" "$@"
else
    gradle "$@"
fi
